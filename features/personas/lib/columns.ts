/** Columns loaded anywhere persona prompt data is needed. */
export const PERSONA_SELECT =
  "id, user_id, name, slug, base_tone, relationship_type, world_type, static_prompt_json, version, created_at, updated_at, deleted_at";
